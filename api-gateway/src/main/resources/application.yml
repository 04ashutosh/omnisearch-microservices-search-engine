server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: crawl-service
          uri: http://localhost:8081
          predicates:
            - Path=/crawl/**

        - id: search-service
          uri: http://localhost:8082
          predicates:
            - Path=/search/**